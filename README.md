This is a simple web app built using HTML, CSS and JavaScript.
It allows users to toggle between light and dark mode.
The selected theme is saved using localStorage.

Features
- Dark/Light theme toggle
- Clean UI
- Responsive layout
- Theme saved in browser storage
